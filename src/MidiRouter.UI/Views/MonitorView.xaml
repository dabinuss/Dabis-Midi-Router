<UserControl x:Class="MidiRouter.UI.Views.MonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <ToggleButton Content="Pause"
                          IsChecked="{Binding IsPaused, Mode=TwoWay}"
                          Width="120"
                          Margin="0,0,8,0" />
            <Button Content="Clear"
                    Command="{Binding ClearCommand}"
                    Width="100" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ItemsSource="{Binding TrafficSnapshots}"
                  IsReadOnly="True"
                  MaxHeight="160">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Endpoint" Binding="{Binding EndpointId}" Width="2*" />
                <DataGridTextColumn Header="Msgs/s" Binding="{Binding MessagesPerSecond, StringFormat=F1}" Width="*" />
                <DataGridTextColumn Header="Bytes/s" Binding="{Binding BytesPerSecond, StringFormat=F1}" Width="*" />
                <DataGridTextColumn Header="Channels" Binding="{Binding ActiveChannelsText}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <DataGrid Grid.Row="4"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ItemsSource="{Binding Entries}"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Zeit" Binding="{Binding Timestamp}" Width="2*" />
                <DataGridTextColumn Header="Endpoint" Binding="{Binding EndpointName}" Width="2*" />
                <DataGridTextColumn Header="Channel" Binding="{Binding Channel}" Width="*" />
                <DataGridTextColumn Header="Typ" Binding="{Binding MessageType}" Width="*" />
                <DataGridTextColumn Header="Detail" Binding="{Binding Detail}" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
